---
name: name_server
uri: druby://localhost:12349
notify:
  - address: 224.0.0.1
    port: 54321
  - address: 224.0.0.1
    port: 54322
filters:
  - [ ~, ~ ]
  - [ ~, ~, ~]
  - [ ~, ~, ~, ~]
  - [ ~, ~, ~, ~, ~]
adapter:
  host: localhost
  port: 8004
  max_clients: 32